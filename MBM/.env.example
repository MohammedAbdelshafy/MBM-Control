# MBM Mission Control - Environment Variables
# Copy this file to .env and insert valid credentials before executing BAGA-001.

# [REQUIRED] PropStream API - Used by evidence-collector for signal stacking (Absentee + 7yrs + Equity + Distress).
PROPSTREAM_API_KEY=""

# [REQUIRED] Skip Tracing API (Twilio / BatchLeads / etc) - Used by revenue-review to verify contacts and drop duds.
SKIP_TRACING_API_KEY=""

# [OPTIONAL] LLM APIs - If local Ollama models are insufficient for complex QA reasoning.
OPENAI_API_KEY=""
ANTHROPIC_API_KEY=""
